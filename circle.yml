#basic setup
machine:
  timezone: Asia/Tokyo
  post:
    - sudo dnf install jq

# test
test:
  override:
    - echo "test"

# deploy
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync project s3://vivo-sample-web/ --delete
